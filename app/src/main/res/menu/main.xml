<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    >
    <item
        android:id="@+id/item_menu_setting"
        android:title="@string/item_menu_setting"
        android:icon="@mipmap/ic_launcher"
        android:showAsAction="never"
        />

    <item
        android:id="@+id/item_menu_sync"
        android:title="@string/item_menu_sync"
        android:icon="@mipmap/ic_launcher"
        android:showAsAction="never"
        />

    <item
        android:id="@+id/item_menu_clearcompleted"
        android:title="@string/item_menu_clearcomplete"
        android:icon="@mipmap/ic_launcher"
        android:showAsAction="never"
        />

</menu>